pipeline
{
	agent any
	stages {
		stage('Checkout') {
			steps {
				  checkout scm
				  }
		}
		
		stage('Build') {
			steps {
					bat 'powershell ".\\build\\build.ps1 $env:BUILD_NUMBER"'
				  }
		}

		stage('Test') {
			steps 
				  {
					powershell '.\\build\\test.ps1'
				  }
		}
	}   
}